namespace cpu_monitor.msg;

table ThreadInfo {
  name: string;
  id: uint64;
  usage: float32;
  timestamps: uint64;
}

table ProgressInfo {
  id: uint64;
  name: string;
  infos: [ThreadInfo];
}

table ProgressMsg {
  infos: [ProgressInfo];
  timestamps: uint64;
}

root_type ProgressMsg;
